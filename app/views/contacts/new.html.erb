<h1 id="page-title">Hello mafrend</h1>

<div class="wrapper">
    <%= form_for @contact do |f| %>
        <%= f.label :name %><br>
        <%= f.text_field :name, required: true %>

        <br>

        <%= f.label :email %><br>
        <%= f.email_field :email, required: true %>

        <br>

        <%= f.label :message %><br>
        <%= f.text_area :message, as: :text %>

        <%= f.submit 'Send Message', class:"button" %>
    <% end %>
</div>